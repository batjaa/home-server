---
internal_swag_ip: '{{ swag_base }}.34'

internal_macvlan_ip: '{{ swag_base }}.62/32'

# Internal-only vhosts are reachable from the LAN and from Tailscale.
# Tailscale client IPs live in 100.64.0.0/10.
swag_internal_allow_cidrs:
  - "{{ lan_network }}"
  - "100.64.0.0/10"
