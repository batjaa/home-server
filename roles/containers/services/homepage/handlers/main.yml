---
# Plain `docker restart` — calling the docker_container module here would
# treat the existing volume/network/device config as drift (since it's not
# in this stub spec) and recreate the container without them.
- name: restart homepage
  command: docker restart homepage
  changed_when: true
